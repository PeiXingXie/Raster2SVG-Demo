{
  "name": "shape-studio-desktop-client",
  "version": "0.3.0",
  "private": true,
  "author": "Shape Studio Contributors",
  "description": "Desktop app for turning raster images into editable shape-based SVG assets.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "start:dev": "electron . --dev",
    "dist": "electron-builder"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "com.local.shapestudio",
    "productName": "Shape Studio",
    "artifactName": "${productName} Setup ${version}.${ext}",
    "directories": {
      "output": "../dist/installers"
    },
    "files": [
      "main.js",
      "preload.js",
      "assets/icon.png",
      "assets/icon.ico",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../dist/backend",
        "to": "backend",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "icon": "assets/icon.ico",
      "target": [
        "nsis"
      ]
    },
    "mac": {
      "target": [
        "dmg"
      ]
    },
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Graphics"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "include": "build/installer.nsh",
      "installerIcon": "assets/icon.ico",
      "uninstallerIcon": "assets/icon.ico",
      "allowToChangeInstallationDirectory": true,
      "createStartMenuShortcut": true,
      "menuCategory": true,
      "deleteAppDataOnUninstall": false
    }
  }
}
